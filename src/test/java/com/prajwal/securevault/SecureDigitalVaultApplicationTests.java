package com.prajwal.securevault;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = SecureDigitalVaultApplication.class)
public class SecureDigitalVaultApplicationTests {

	@Test
	void contextLoads() {
		// This test checks if the Spring application context loads successfully
	}
}
